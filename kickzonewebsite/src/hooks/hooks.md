hooks/ = " logic tái sử dụng "
 - Là các hàm logic dùng lại nhiều lần
  ví dụ : 
    + useAuth() -> kiểm ra logic
    + user Debounce() -> chống spam input
    + useFetch() -> gọi API

    Hiểu đơn giản
    - Không phải UI , chỉ là "logic thông minh"
    