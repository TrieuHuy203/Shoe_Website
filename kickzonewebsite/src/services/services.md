service/ = " Gọi API "
    - Nơi giao tiếp với backend
    ví dụ :
        - authService.login()
        - productService.getAll() 

        Quan trọng:
        - UI không gọi API trực tiếp mà phải thông qua Services